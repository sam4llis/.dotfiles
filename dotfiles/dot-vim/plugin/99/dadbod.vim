" Dadbod-UI
let g:db_ui_use_nerd_fonts = 1           " Use Nerd Fonts.
let g:db_ui_show_database_icon = 1       " Show database icons.
let g:db_ui_show_help = 0                " Do not show help (pressing ? for help still works).
let g:db_ui_force_echo_notifications = 1 " Do not show pop-up windows for messages.
let g:db_ui_execute_on_save = 0          " Do not run on save.
let g:db_ui_win_position = 'right'       " Move DBUI interface to the right-hand side.
